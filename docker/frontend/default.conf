server {
        listen 80;
        listen [::]:80;

        server_name ldap_group_front;

        root /var/www/groupe-ldap-front;

        index index.html;

        location / {
                try_files $uri $uri/ /index.html;
        }
}
